import random

secret = random.randint(1, 100)
guess = 0

print("1부터 100 사이의 숫자를 맞혀보세요!")

while guess != secret:
    guess = int(input("숫자 입력: "))
    if guess < secret:
        print("더 큰 수!")
    elif guess > secret:
        print("더 작은 수!")
    else:
        print("정답!")
